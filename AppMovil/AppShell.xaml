<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppMovil.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppMovil"
    xmlns:viewModels="clr-namespace:AppMovil.ViewModels"
    xmlns:pages="clr-namespace:AppMovil.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="Deportivo">

    <Shell.BindingContext>
        <viewModels:AppShellViewModel/>
    </Shell.BindingContext>

    <!-- Header del Flyout personalizado -->
    <Shell.FlyoutHeader>
        <Grid BackgroundColor="#2196F3" HeightRequest="120">
            <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Label Text="📚" FontSize="40" HorizontalOptions="Center" TextColor="White"/>
                <Label Text="Biblioteca Digital" FontSize="18" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center"/>
            </StackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <!-- Pantalla de Login -->
    <ShellContent
         Title="Login"
         ContentTemplate="{DataTemplate pages:LoginPage}"
         Route="LoginPage"
         FlyoutItemIsVisible="False" />
    <ShellContent
         Title="Reseteo de Password"
         ContentTemplate="{DataTemplate pages:RecuperarPasswordPage}"
         Route="ResetPassword"
         FlyoutItemIsVisible="False" />
    <ShellContent
         Title="Crear cuenta"
         ContentTemplate="{DataTemplate pages:RegistrarsePage}"
         Route="RegistrarsePage"
         FlyoutItemIsVisible="False" />

    <!-- Menu Principal - Solo visible cuando está logueado -->
    <FlyoutItem Title="🏠 Inicio" Icon="home.png" >
        <ShellContent
         Title="Dashboard"
         ContentTemplate="{DataTemplate pages:MainPage}"
         Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="📖 Buscar Actividades" Icon="search.png">
        <ShellContent
        Title="Buscar Actividades"
        ContentTemplate="{DataTemplate pages:BuscarActividadesPage}"
        Route="BuscarActividadesPage" />
    </FlyoutItem>


    <!-- Footer del Flyout con opción de logout -->
    <Shell.FlyoutFooter>
        <StackLayout BackgroundColor="#F5F5F5" Padding="10">
            <Button 
             Text="🚪 Cerrar Sesion" 
             BackgroundColor="#F44336" 
             TextColor="White"
             Command="{Binding LogoutCommand}"
             CornerRadius="5"/>
        </StackLayout>
    </Shell.FlyoutFooter>

</Shell>
